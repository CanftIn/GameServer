#include "NetworkBuffer.h"

#include <string>
#include <cstring>
#include <algorithm>

#include "Packet.h"
#include "utils/Log.h"

void SendData(SendNetworkBuffer* send_buff, RecvNetworkBuffer* recv_buff) {
  // char* send_buff = nullptr;
  // char* recv_buff = nullptr;

}

int main() {
  return 0;
}